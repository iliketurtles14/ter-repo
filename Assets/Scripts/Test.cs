using UnityEngine;

public class Test : MonoBehaviour
{
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.F1))
        {
            //Resources.Load<Texture2D>("Center Perks Tileset/tile000") = DataSender.instance.GetComponent<DataSender>().TileList[96].texture;
        }
    }
}

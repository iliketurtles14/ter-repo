# Copilot Instructions

## Project Guidelines
- User prefers minimal, targeted code changes instead of large refactors when fixing bugs.Debug.Log($"StashAnim paused={pc != null && pc.isPaused}, dt={Time.deltaTime}, udt={Time.unscaledDeltaTime}");